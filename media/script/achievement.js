jQuery(function(){
    jQuery("#alumni_achievers").innerfade({
      animationtype: 'fade',
      speed: 'slow',
      timeout: 3000,
      type: 'sequence',
      containerheight: '250px'
    });
});
